{
  "name": "pineneedledesigns-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp ../CNAME ../docs/CNAME && mkdir -p ../docs/images ../docs/videos && rsync -a --delete images/ ../docs/images/ && rsync -a --delete --include='*/' --include='*.webm' --exclude='*' ../videos/ ../docs/videos/ 2>/dev/null || true",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "bootstrap-icons": "^1.13.1",
    "pinia": "^3.0.4",
    "vue": "^3.4.4",
    "vue-router": "^4.3.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "vite": "^8.1.3"
  }
}
